import React, {
  type ReactNode
} from 'react';
import {
  Button
} from 'antd';

import Style from './index.module.css';

export default function Footer(prop: any): ReactNode {
  return <div className={ Style.footer }>

  </div>;
}